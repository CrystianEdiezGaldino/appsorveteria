import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(252, 200, 0, 1);
const secondaryColor = Color.fromRGBO(101, 2, 183, 1);
const textColor = Color.fromRGBO(223, 232, 240, 1);
const bgColor = Color.fromRGBO(45, 2, 148, 1);

const defaultPadding = 16.0;
